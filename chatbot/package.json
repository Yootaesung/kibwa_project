{
  "name": "kibwa-chatbot",
  "version": "1.0.0",
  "description": "Kibwa Chatbot with FastAPI",
  "main": "app.py",
  "scripts": {
    "start": "uvicorn app:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn app:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "pm2": "^5.3.0"
  }
}
